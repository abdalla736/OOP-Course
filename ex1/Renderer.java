/**
 * interface that represents renderer
 */
public interface Renderer {
    /**
     * renders the given board to the user.
     *
     * @param board the board to render
     */
    void renderBoard(Board board);
}
